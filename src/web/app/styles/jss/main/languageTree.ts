
export const getStyles = (theme: any) => ({
  root: {
    maxHeight: '30%',
    overflow: 'auto'
  },
  languagesIcon: {
    color: 'rgba(0, 0, 0, 0.7)'
  },
  filteredIcon: {
    marginRight: 5,
    color: '#1890ff',
    minWidth: 'unset'
  }
});

export default getStyles;